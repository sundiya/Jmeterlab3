-- 创建第一个数据库 db_student1
CREATE DATABASE IF NOT EXISTS db_student1;
USE db_student1;

-- 创建以 A 开头的学生表
CREATE TABLE IF NOT EXISTS student_A (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_B (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_C (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_D (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_E (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_F (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_G (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_H (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_I (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_J (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
CREATE TABLE IF NOT EXISTS student_K (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_L (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_M (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_N (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_O (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_P (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Q (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_R (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_S (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_T (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
CREATE TABLE IF NOT EXISTS student_U (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_V (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_W (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_X (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Y (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Z (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
-- 创建第二个数据库 db_student2
CREATE DATABASE IF NOT EXISTS db_student2;
USE db_student2;

-- 创建以 A 开头的学生表
CREATE TABLE IF NOT EXISTS student_A (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_B (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_C (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_D (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_E (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_F (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_G (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_H (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_I (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_J (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
CREATE TABLE IF NOT EXISTS student_K (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_L (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_M (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_N (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_O (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_P (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Q (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_R (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_S (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_T (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
CREATE TABLE IF NOT EXISTS student_U (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_V (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_W (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_X (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Y (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Z (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
-- 创建第三个数据库 db_student3
CREATE DATABASE IF NOT EXISTS db_student3;
USE db_student3;

-- 创建以 A 开头的学生表
CREATE TABLE IF NOT EXISTS student_A (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_B (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_C (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_D (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_E (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_F (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_G (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_H (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_I (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_J (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
CREATE TABLE IF NOT EXISTS student_K (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_L (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_M (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_N (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_O (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_P (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Q (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_R (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_S (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_T (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
CREATE TABLE IF NOT EXISTS student_U (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_V (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_W (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_X (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Y (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

CREATE TABLE IF NOT EXISTS student_Z (
                                         id INT NOT NULL AUTO_INCREMENT,
                                         name VARCHAR(255) NOT NULL,
    passport_number VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );
